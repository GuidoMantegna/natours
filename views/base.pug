doctype html
html
  head
    title Natours | #{tour}
    link(rel='stylesheet' href='/css/style.css')
    link(rel='shortcut icon' type='image/png' href='img/favicon.png')
  
  body
    h1= tour
    // We can also write JS here
    h2= user.toUpperCase()
    //- h1 The Park Camper

    //- unbuffered code
    - const x = 9
    //- buffered code
    h2= 2 * x